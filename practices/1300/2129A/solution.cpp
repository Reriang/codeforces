#include <iostream>
#include <vector>
#include <utility>
using namespace std;

/*
Codeforces: 2129A. Double Perspective
Link: https://codeforces.com/problemset/problem/2129/A
*/

void solve() {
    size_t n; cin >> n;

    vector<pair<size_t, size_t>> edges(n);
    for (auto &e : edges) cin >> e.first >> e.second;

    
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t; cin >> t;

    while (t--) solve();

    return 0;
}
